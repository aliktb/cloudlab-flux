apiVersion: kustomize.config.k8s.io/v1beta1
configMapGenerator:
- behavior: create
  files:
  - conf/values.yaml
  name: loki-config
  options:
    disableNameSuffixHash: true
kind: Kustomization
namespace: monitoring
resources:
- manifests
