apiVersion: kustomize.config.k8s.io/v1beta1
configMapGenerator:
- name: external-dns-config
  files:
  - values.yaml=conf/external-dns-config.yaml
generatorOptions:
  disableNameSuffixHash: true
kind: Kustomization
namespace: external-dns
resources:
- manifests
