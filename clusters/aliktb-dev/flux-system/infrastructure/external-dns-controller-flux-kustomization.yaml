apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: external-dns-controller
  namespace: flux-system
spec:
  dependsOn:
  - name: external-secrets-operator-config
  interval: 5m
  path: "./infrastructure/external-dns/controller/kustomize/dev"
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
